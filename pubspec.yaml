name: flux
description: Flux Flutter app scaffold (offline-first, mock data)
version: 0.1.0
publish_to: 'none'
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  go_router: ^6.0.0
  flutter_riverpod: ^2.1.0
  riverpod: ^2.0.0
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.0
  background_downloader: ^9.5.4
  file_picker: ^9.0.0
  record: ^6.2.0
  speech_to_text: ^7.0.0
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
